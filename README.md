# PythonProject9.1lesson

🧩 Учебный Python-проект с модулями, тестами и настройками окружения.  
Проект создан в рамках обучения и демонстрирует структуру реального приложения: модули, юнит-тесты, виртуальное окружение. Состоит из модулей:

`widget.py` - Маскирует номер карты или счёта в зависимости от типа. Использует готовые функции get_mask_card_number и get_mask_account.

`masks.py` -  Маскирует номер банковской карты: XXXX XX** **** XXXX, Маскирует номер счёта: **XXXX

`processing.py` - Возвращает список операций, у которых ключ 'state' равен заданному значению.  Возвращает список операций, отсортированный по дате (по умолчанию — от новых к старым).


---

## 🚀 Основные возможности

- ✅ **Модули**: `masks.py`, `widget.py`, `processing.py` — логика приложения
- ✅ **Тесты**: `test_module.py` — проверка функций с помощью `pytest` и др.
- ✅ **Виртуальное окружение**: `.venv` — изолированная среда с зависимостями
- ✅ **Инструменты**: `pre-commit`, `black`, `isort`, `flake8`, `mypy` — автоматическая проверка кода
- ✅ **Структура**: чистая, масштабируемая

---

## 📁 Структура проекта

PythonProject9.1lesson/

├── src/ # Исходный код

│ ├── processing.py # Обработки данных

│ ├── masks.py # Работа с масками (например, банковские реквизиты)

│ ├── widget.py # Виджет-логика

│ └── init.py # Пакет Python

├── tests/ # Тесты

│ ├── pycache/ # Кэш тестов

│ ├── test_module.py # Юнит-тесты

│ └── init.py # Пакет тестов

├── .venv/ # Виртуальное окружение (не пушится)

└── README.md # Этот файл

---

---

## ⚙️ Как запустить

1. Убедись, что у тебя установлен **Python 3.14+**
2. Активируй виртуальное окружение:

   ```bash
   .venv\Scripts\Activate.ps1  # Windows (PowerShell)
   
3. Установи зависимости (если нужно):

   ```bash
   pip install -r requirements.txt  # если файл есть
4. Запусти тесты:

   ```bash
   pytest tests/

5. Запусти модуль:

   ```bash
   python src/widget.py
   
## Как форматировать код
Проект поддерживает автоматическую чистку:

```bash
### Отформатировать весь код
black src/ tests/
isort src/ tests/

### Проверить стиль
flake8 src/ tests/

### Проверить типы
mypy src/
Или используй pre-commit, чтобы всё делалось автоматически при коммите.
```

## Тестирование

```bash
pytest tests/ -v

Тесты находятся в папке tests/

Используется pytest — современный фреймворк для тестов

Покрытие можно расширить при развитии проекта
```

 ## Инструменты в .venv
Установлены:

black — форматирование кода

isort — сортировка импортов

flake8 — проверка стиля

mypy — статическая типизация

pre-commit — хуки до коммита

pytest — запуск тестов

 ## Лицензия
Проект — учебный.

Можно использовать как шаблон.

Автор: caninkostya-wq

Дата: декабрь 2025
